define([
  'config/global'
], function (g) {
  'use strict';

  /**
   * TODO. TODO. TODO.
   * A datacube.Component. Used in datasets' structure.
   */
  var Component = function (identifier) {
    var self = this;

    self.identifier = identifier;

  };

  return Component;
});
